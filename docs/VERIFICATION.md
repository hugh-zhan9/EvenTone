# MVP 验证记录

更新：2026-09-17；初版记录：2026-09-16。环境：本机 macOS 27.0（26A428）、Apple Swift 6.3.3、arm64。新建项目，没有已有业务测试基线、没有修改其他项目。

## 已验证

- `./scripts/test.sh`：11 / 11 通过。C 使用 `-Wall -Wextra -Werror`，Swift 测试使用 `-warnings-as-errors`。
- `swift build -c release -Xswiftc -warnings-as-errors`：通过。
- `./scripts/build.sh`：release 编译、图标生成、Info.plist 校验、本机 ad-hoc 签名与签名校验通过。
- `bash -n scripts/build.sh scripts/test.sh`：通过。
- `clang --analyze ... Sources/AudioDSP/AudioDSP.c`：无诊断。
- 打包程序 `--diagnose` 识别当前默认输出「外置耳机」，44,100 Hz，2 通道、1 输出流。该命令不建立 tap。
- 原生 UI 已启动并视觉检查：菜单栏、整体音量、均衡开关、设备补偿、状态和退出可见；初始总开关为关闭。
- 新版在启动/清理 HAL 等待期间，UI 可读、退出按钮可点击；点击退出后 `pgrep` 确认本项目应用进程已结束。
- 独立只读审查发现并修复：HAL 释放失败时保留仍可能被回调访问的内存；完整音频格式指纹；所有 HAL 查询、监听与启停在专用串行队列。复审未发现新的 Critical / Important。复审不是实机音频验证。

11 项场景覆盖：相差约 24 dB 的合成输入收敛、轻→响突变及峰值上限、静音和极低底噪、手动补偿/音量、立体声比例、非法浮点/空输入/单帧/单声道、最大增益/静音音量、跳过物理麦克风缓冲并映射 planar 输出、异常缓冲格式、设备设置独立持久化、非法设置约束。

测试执行器在开发中遇到失败断言时以 exit 133 退出，编译失败以非零状态退出；最终正常测试以 exit 0 结束。首轮 XCTest 无法运行的原因是 CLT 不含 XCTest，完整 Xcode 尚未接受许可。未代用户接受许可，改用独立测试执行器，保留全部场景。

## 2026-09-17 · 授权后实机回归

用户确认已允许 EvenTone 系统音频录制。使用同一路径的应用验证，未代操作系统权限，也未重启音频服务、修改默认输出或硬件音量。测试过程中界面先后观察到「还得是原道」48 kHz 和「外置耳机」44.1 kHz；以下成功电平验证针对后者，不据此声称两个设备均完成验证。

### 修复与基线

授权前，HAL 曾出现 30 秒级等待、没有回调；此前已将所有 HAL 操作移至独立串行队列。授权后，初始化/清理等待缩短，外置耳机通路可接收合成参考音。仍发现设备重建后无回调超过 5 秒时，被应用自己的 watchdog 关闭。无回调不够证明 HAL 故障，因此删除这条自动关闭规则：通路成功开启后，空闲保持等待；实际 Core Audio 错误和缓冲格式错误仍报告并清理。

保留回调新鲜度仅供界面使用：超过 0.5 秒无新回调时隐藏过期电平，不把最后一个电平当作仍在播放。修复前 AppModel 保存在 `.build/baseline-20260917/AppModel.swift` 供本轮差异检查；该临时目录不作为长期交付依赖。

### 最终构建的实际观察

整体音量为 80%，设备补偿 0 dB；参考声是本地生成的 440 Hz 立体声正弦，48 kHz PCM，未录制用户音频。电平由应用的实际 Core Audio IO 回调计算并通过原生 UI 读取。

| 检查 | 观察结果 |
| --- | --- |
| 开启后无内容等待 12 秒 | 总开关保持开启，显示「已启动 · 等待音频」，未误关闭 |
| 随后播放较轻参考声 | 自动恢复「正在处理音频」；输入 -30.9 dBFS，自动增益 +9.0 dB，输出 -23.9 dBFS |
| 播放较响参考声 | 输入 -21.4 dBFS，自动增益 -0.6 dB，输出 -23.9 dBFS |
| 关闭自动均衡，再播放较轻参考声 | 输入 -30.9 dBFS，输出 -32.8 dBFS，符合 80% 音量的约 -1.94 dB 衰减 |
| 当前设备补偿改为 -3 dB | 输出 -35.9 dBFS，相对未补偿下降约 3 dB |
| 关闭总开关 | 界面变为已关闭；AudioDeviceStop 与 DestroyIOProcID 返回 0，未显示清理错误 |

上述验证证明数字音频捕获与处理通路工作，不等同于测量耳内声压或确认主观听感。测试结束恢复补偿 0 dB、自动均衡开启、整体音量 80%、总开关关闭。

修改后再次运行 `./scripts/test.sh`，11 / 11 通过；`./scripts/build.sh` 构建、打包与签名校验通过。独立只读复审本次 AppModel / ControlView 差异未发现新增 Critical / Important。本次空闲缺陷通过上表实机回归验证，未用镜像实现的单元测试替代 HAL 实际行为。

### 重复实机检查

1. 开启应用并等待超过 5 秒；应保持等待，不能自动报错关闭。
2. 播放两段不同音量的参考内容；稳定后比较输出电平，再关闭均衡比较差异。
3. 固定内容与整体音量，将设备补偿降低 3 dB，观察输出相应降低；最后恢复设置。
4. 关闭总开关，检查无清理错误及后续正常播放；主观恢复原声仍可配合试听确认。

## 2026-09-17 · v0.2.0 设备切换与登录启动

依据用户新增要求增加快速切换设备、登录时启动，保留每次打开应用时音频总开关默认关闭的合同。

- 变更前 11 项测试通过；最终 `./scripts/test.sh` 的 20 项测试通过，退出状态为 0。新增覆盖设备切换确认、重复和迟到回调、超时、读取/设置/监听/清理错误，以及登录项注册、取消、待批准、失败和外部状态刷新。
- `swift build -c release -Xswiftc -warnings-as-errors` 通过；`./scripts/build.sh` 完成 v0.2.0（build 2）打包与签名校验。
- 独立只读审查未发现 Critical / Important。切换监听器移除失败会报告错误，但目前不重试该清理操作；回调弱引用避免访问已释放的请求对象。
- 原生界面列出五个可选输出，显示当前勾选项。总开关关闭时，从「还得是原道」切到「外置耳机」，补偿从 +6 dB 变为 0 dB，总开关保持关闭。
- 在外置耳机开启处理后观察到实时电平；总开关开启时切回「还得是原道」，读回 48 kHz、+6 dB，保持开启意图并重建通路。整体音量保持原值 `0.6064005761173186`（界面 60%），自动均衡保持开启。
- 随后系统默认输出再次变成「外置耳机」，只读诊断确认了该变化。尚未确认这是用户手动操作还是系统设备变化，因此未归因，也未再次替用户切回。
- 登录启动在原生 UI 中关闭后读回未注册；再次开启后读回已开启。最终保留开启状态。未执行注销或重启，下次实际登录自动打开仍待正常登录验证。
- 登录项待批准、拒绝/错误，以及设备切换故障通过替身后端测试，未在用户系统做破坏性故障注入。

测试生成的三份合成 WAV 已清理，未录制或保存用户音频。

## 2026-09-17 · 切换按钮对比度修正

用户截图显示深色面板上的原生菜单按钮呈黑色。为切换菜单显式指定深色颜色环境，并为标签指定主题前景色。修改前后 release 编译均通过（warnings-as-errors）；打包与签名校验通过。重新打开应用后的实际截图确认文字、双向箭头和下拉箭头为清晰浅色，点击后仍能展开五个设备选项。当前设备、音量、补偿及登录启动设置保持原值。

## 2026-09-17 · 启动恢复上次总开关

用户明确将启动规则改为“之前开启则自动开启，之前关闭则保持关闭”，替代上文历史验证中的每次启动默认关闭。依据概要设计 D-006，复用 Preferences 和 AppModel 首次设备刷新，无新增权限请求或后台服务。

- 变更前 20 项测试通过；变更后 21 项测试通过，新增默认关闭、保存开启、保存关闭和非法值测试。release 编译（warnings-as-errors）、打包、Info.plist 和签名校验通过。
- 精确差异独立只读审查未发现 Critical / Important；源代码基线暂存在 `.build/startup-baseline-20260917/`。
- 新版没有已有总开关保存值时，界面默认关闭。开启后退出，进程已结束且配置为 1；重开同一包，无需点击总开关即显示正在处理音频和实时电平。
- 关闭后退出，进程已结束且配置为 0；重开同一包仍显示已关闭。之后恢复测试前开启状态，未改整体音量 `0.3047791550279331`（界面 30%）、当前设备「还得是原道」、+6 dB 补偿、自动均衡及登录启动。
- 同一包的重开和再次开启中未观察到重复授权弹窗。当前签名为 adhoc，designated requirement 绑定代码哈希；结合 [Apple TN3127](https://developer.apple.com/documentation/technotes/tn3127-inside-code-signing-requirements)，此前连续重新编译后再授权与签名身份变化相符，但没有捕获每次弹窗，不能断言所有提示都由此引起。未改签名或重置系统授权。
- 未主动注销或重启；实际登录启动待用户下次正常登录验证。错误转关闭的持久化由 AppModel 统一属性观察器覆盖，未对实机注入 HAL 失败。

## 自动发布验证（2026-09-17）

用户确认使用 GitHub Releases + Actions artifacts：main 推送生成预览版，v* 标签生成正式版。构建和发布合同见 D-007。

- 原有 21 项测试及 release 编译（warnings-as-errors）基线通过；此次未修改音频实现。
- 新增发布逻辑 15 项测试通过：main/标签/PR/手动触发、版本不匹配、异常输入、轻量/注解标签、精确提交读回、远端失败不当作标签缺失、上传失败和标签移动不公开草稿、已有发布或草稿拒绝重复创建、按本次草稿 ID 公开。
- 新增打包 4 项测试通过：正式版/预览版 ZIP、路径含空格、校验和、解包签名、拒绝重复输出、缺失应用/错误版本/非法标签及签名损坏。
- `EVENTONE_DIST_DIR=.build/ci-build ./scripts/build.sh` 验证构建到独立目录，未替换本机正在运行的 app；构建、Info.plist、签名检查通过。
- actionlint v1.7.12、shell 语法、Markdown 相对链接与 diff 空白检查通过。
- 独立审查发现“远端标签存在不等于匹配构建提交”，已加入上传前后解引用校验及同标签并发隔离，对应测试已通过。
- 复审发现显式草稿创建不能依赖 CLI 的发布唯一性检查，已增加包含草稿的完整分页查重，以及创建响应和提交校验；使用创建响应返回的 release ID 上传和公开，对应测试已通过。
- 首轮 Actions `35176833676` 的 macOS 构建、测试、打包及 artifact 上传通过；发布成功创建草稿及附件，但随后列表读回未通过唯一性检查，未公开。手动只读查询确认草稿内容及标签提交匹配。已改为直接使用创建 API 返回的 ID，取消创建后再次列表查找，保留创建前查重和上传前后标签校验。
- 云端运行及附件见 [Actions](https://github.com/hugh-zhan9/EvenTone/actions/workflows/release.yml) 与 [Releases](https://github.com/hugh-zhan9/EvenTone/releases)。构建检查不代替硬件音频测试或 Apple 公证。

## 仍需实物 / 长时间验证

- AirPods 与外置耳机主观校准、反复切换、耳内响度一致性。
- 连续视频播放的自然程度、Bluetooth 延迟与音画同步。
- 睡眠唤醒、设备断开、Bluetooth 通话模式及不同硬件格式切换。
- HAL 清理失败故障注入；该路径目前为源码审查覆盖。

产品限制：RMS 估计不是认证 LUFS，采样峰值限幅不是真峰值限幅；只支持默认设备单流单/立体声 Float32。无信号时显示等待，不把此状态当作权限已获批或某个源一定可捕获的证明。
